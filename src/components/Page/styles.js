import styled from 'styled-components'

export const Wrapper = styled.div`
  box-shadow: inset 0 8px 24px 0 rgba(0, 0, 0, 0.02);
  display: flex;
  height: 100%;
  flex-direction: column;
`

export const Main = styled.main``
